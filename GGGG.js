function hide()  {
    let.section = document.querySelectorAll (.section)
    section.for Each (s => {
        s.style.display = 'none'
    }
        )
        
    }
}

function.showssection (section_id) {
    let.section = document.getElementsByName(action_id)
    if (section) {
        section.style.display = 'block';
    }
    


}

function.final(section_id) {
    showssection ()
    showssection (section_id)
}